{
  "name": "storex",
  "version": "1.0.0",
  "description": "Storage services",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . Storex --overwrite --asar=true --platform=win32 --arch=x64 --prune=true --out=release-builds"
  },
  "author": "AV",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.2.1",
    "node-gyp": "^3.6.2",
    "sqlite3": "^3.1.13"
  },
  "devDependencies": {
    "electron": "^1.7.10",
    "electron-builder": "^19.52.1"
  }
}
